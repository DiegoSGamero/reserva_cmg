<div class="container container-home d-flex p-4" data-controller="popup">
  <div class="col-3"></div>
  <div class="col-6 m-3 text-center main-box">
    <div class="home-title">
      <h1> Faça sua reserva aqui! <h1>
    </div>
    <div class="reserve-container">
        <div class="reservation-form">
          <%= simple_form_for @reservation, url: reservations_path do |f| %>
          <div>
            <%= f.input :reserve_name, label: "Nome da Reserva" %>
          </div>

          <div>
            <%= f.input :date_only, as: :date, label: "Data" %>
          </div>

          <div>
            <%= f.input :time_only, as: :time, label: "Horário" %>
          </div>

          <div>
            <%= f.input :reservation_type, collection: Reservation.reservation_types.keys, label: "Tipo de Reserva" %>
          </div>

          <div>
            <%= f.input :details, label: "Detalhes" %>
          </div>

          <%= f.button :submit, "Reservar", class: "btn" %>
        <% end %>
      </div>
    </div>
    <!-- Pop-up de confirmação -->
  <%# <div data-popup-target="popupWindow" class="popup-create" style="display: none;">
    <div class="popup-content">
      <span data-confirmation-target="close" class="close-popup">&times;</span>
      <h2>Reserva realizada com sucesso!</h2>
      <p>Você deseja realizar outra reserva?</p>
      <button data-confirmation-target="confirm" type="button">Sim</button>
      <button data-confirmation-target="cancel" type="button">Não</button>
    </div>
  </div> %>
  <div class="col-3"></div>
</div>
